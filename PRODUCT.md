# VisonCube Web 产品说明

## 1. 产品定位

VisonCube Web 是 VisonCube 产品体系的统一官网和账户入口。网站登录后展示三个软件、提供可信的官方下载入口，并复用现有 VisonCube 账户服务，不创建第二套账号系统。

当前阶段先确认品牌首页和三个产品页的视觉方向，不接入后端、不发布线上。

## 2. 页面与产品命名

页面名称已经确定，后续界面、路由、文档和导航必须保持一致：

| 页面 | 对应产品 | 支持平台 | 建议路由 |
| --- | --- | --- | --- |
| 首页 | VisonCube 产品总入口 | Web | `/` |
| 观山 | VisonCube-TV | Android | `/guanshan` |
| 听雨 | VisonCube-Music | Android、Windows | `/tingyu` |
| 逐风 | VisonCube-AI | Windows | `/zhufeng` |

未登录用户先进入 VisonCube 登录门禁，顶部只显示 `VisonCube`，不显示“首页、观山、听雨、逐风”。登录成功后才进入四个品牌页面。全局账户入口建议使用 `/account`，登录、注册、邮箱验证码登录和找回密码使用账户模块内的独立路由。

## 3. 目标用户

- 已经使用 VisonCube-TV 或 VisonCube-Music，希望下载新版本、登录同一账户的用户。
- 第一次了解 VisonCube，希望快速判断三个产品分别解决什么问题的用户。
- 在手机上查找 Android 安装包，或在电脑上查找 Windows 安装包的用户。

## 4. 第一阶段范围

### 4.1 品牌页面

- 品牌页面要求登录后访问；未登录访问时统一回到 VisonCube 登录门禁。
- 首页展示 VisonCube 品牌和三个产品入口。
- 观山展示影视观看能力、Android 平台和版本下载入口。
- 听雨展示音乐播放、Android/Windows 双端和云端历史/收藏同步。
- 逐风展示 AI Windows 产品，但暂不接入客户端登录。

### 4.2 统一账户

网站后续复用现有认证接口，包含：

- 用户名或邮箱加密码登录；
- 邮箱验证码登录；
- 用户名、必填邮箱、邮箱验证码和密码注册；
- 找回密码；
- 账户信息、邮箱、修改密码和退出登录。

### 4.3 下载与版本

- 下载信息必须读取 VisonCube 服务端发布配置，不在页面中永久写死版本和文件地址。
- 对外显示公开版本；内部更新版本只用于客户端更新比较，不出现在普通用户界面。
- Android 和 Windows 下载按钮必须明确标注平台、架构或安装包类型。
- 首页只负责进入产品页，不集中堆放安装包；观山、听雨、逐风分别在自己的页面提供下载入口。
- 网站只保存发布客户端编号，真实下载地址使用接口返回的 `download_url`。

发布客户端编号固定为：

| 页面 | 平台 | client_id |
| --- | --- | --- |
| 观山 | Android | `tv-android` |
| 听雨 | Android | `music-android` |
| 听雨 | Windows x64 | `music-windows` |
| 逐风 | Windows x64 | `ai-windows` |

统一发布接口为 `GET /api/releases/{client_id}`。现有 TV 客户端使用的 `GET /api/release` 继续保留，不因网站接入而删除或改变。

发布接口和静态安装包保持公开，以支持客户端在登录前检查更新；网站页面本身仍受登录门禁保护。AI 没有正式发布包时，逐风页面显示“暂未提供下载”，不得伪造地址。

## 5. 当前原型交付

本轮只交付：

- `PRODUCT.md`；
- `DESIGN.md`；
- 一个无依赖的首页静态视觉原型；
- 观山、听雨、逐风三个产品页静态原型；
- 登录页、首页和产品页的桌面端与手机端截图。

原型中的按钮和产品路由仅用于视觉确认，不代表功能已经实现。

原型默认展示未登录状态；使用 `?state=member` 仅用于预览登录后的产品主界面，不代表真实登录。

## 6. 非目标

第一阶段不包含：

- VisonCube-AI 客户端登录接入；
- 支付、会员、社区、评论和论坛；
- 管理员后台；
- 云端模型上传与分发；
- PostgreSQL 迁移；
- 线上服务器、DNS 或 Nginx 修改。

## 7. 技术与部署约束

- 生产网站后续优先采用 Vue 3、TypeScript 和 Vite，构建为静态文件交给 Nginx，提高慢服务器下的响应稳定性。
- 认证和发布 API 继续使用 `https://linux.sjmf.xyz`，正式接入网站时再配置精确 CORS 白名单。
- 不在前端源码、截图、日志或文档中记录密码、SMTP 授权码、Cookie 密钥和访问令牌。
- 图片和动画需要按需加载，首屏不依赖大体积自动播放视频。
- 网站不显示随软件版本变化的应用图标和软件截图；产品识别只使用 `VisonCube`、`观山`、`听雨`、`逐风` 名称和各页面固定的色彩/动效语言。

## 8. 首轮验收标准

- 未登录状态顶部只显示 `VisonCube`，页面可见文本中不得出现“首页、观山、听雨、逐风”。
- 登录状态导航准确显示“首页、观山、听雨、逐风、账户”。
- 首页在桌面和手机上没有横向溢出、文字遮挡或大片无意义空白。
- 用户能在首屏理解三个页面与三个软件的对应关系。
- 每个产品页的下载按钮准确标明平台和安装包类型，并具备加载、失败、可下载和暂未发布状态。
- 风格可辨认地借鉴 Raycast 的深色层级与产品展示方式，但不复制其品牌、图标、文案或具体组件。
- 原型不访问生产 API，也不修改任何线上环境。
