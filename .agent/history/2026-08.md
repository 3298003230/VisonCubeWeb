# 2026-08

## 2026-08-02：观山场景轻量化

- 反馈：照片背景加载慢，且与听雨、逐风的抽象 CSS 风格不一致。
- 完成：移除正式页面与 prototype 对 `guanshan-scene.png` 的引用，改用渐变、网格、远山轮廓和雾光构成轻量场景；不再显示 16:9 占位框。
- 验证：`npm.cmd run typecheck`、`npm.cmd run build` 通过；dist 不再包含山景图片，构建产物约 150KB。
- 交付：生成 `staging/VisonCubeWeb-1.0.1-light-scene.zip` 并完成线上替换；旧版本备份在 `/var/backups/visoncube-web-20260802-022818`。
- 线上验证：`https://sjmf.xyz/` 返回 200，`https://sjmf.xyz/api/releases/music-android` 返回 200。

## 2026-08-02：网站首次线上部署

- 目标：将 VisonCube Web 部署到独立域名 `sjmf.xyz`，AI 下载入口暂不加入。
- 完成：上传并部署 Vite 构建产物到 `/var/www/visoncube-web`；新增独立 Nginx 站点；`/api/` 反向代理到现有认证服务；HTTP 自动跳转 HTTPS。
- 证书：通过 Certbot 手动 DNS TXT 验证签发 `sjmf.xyz` 证书，有效期至 2026-10-30。
- 验证：HTTPS 首页 200、API 200、HTTP 301、JS/CSS 资源 200；`linux.sjmf.xyz` 原有配置未改动。
- 剩余：真实浏览器登录注册验收和证书自动续期仍待后续处理。

## 2026-08-02：观山视觉二次调整

- 反馈：上一版仍保留原有几何构图，16:9 空框与山景脱节，整体不够自然。
- 完成：新增 `prototype/assets/guanshan-scene.png` 山景氛围图；在正式 Vue 与 prototype 样式中将其作为观山背景，并隐藏原 CSS 山体、网格和 16:9 占位框。
- 验证：`npm.cmd run typecheck`、`npm.cmd run build` 通过，Vite 已生成对应图片资源。
- 边界：未修改听雨、逐风、下载接口、认证逻辑或线上环境。

## 2026-08-02：观山场景视觉优化

- 目标：改善观山产品页右侧场景中生硬的大三角山体、过重的前景黑块和突兀的 16:9 播放框。
- 完成：同步调整 `src/styles/main.css` 与 `prototype/styles.css`，增加柔和远山光晕和雾化层，重绘多点位山体轮廓，降低网格存在感，并统一播放器框的层次和阴影。
- 验证：`npm.cmd run typecheck`、`npm.cmd run build` 通过；本地预览页可正常打开。
- 边界：未修改听雨、逐风场景、接口、下载逻辑或线上环境。

## 2026-08-01：首页视觉方向与原型

- 目标：确认 VisonCube 官网页面命名和 Raycast 风格首页方向，先交付文档与截图。
- 完成：创建 `PRODUCT.md`、`DESIGN.md` 和无依赖首页静态原型；复用现有 VisonCube 与 Music 图标，设计首页、观山、听雨、逐风和账户导航。
- 验证：真实浏览器按桌面与手机尺寸渲染；图片均加载成功，无控制台错误和横向溢出；输出 `design/previews/home-desktop.png` 与 `home-mobile.png`。
- 剩余：等待用户确认首页视觉与文案，尚未搭建正式前端工程或接入后端。

## 2026-08-01：未登录门禁原型

- 完成：默认访客态顶部只显示 VisonCube，新增密码登录/验证码登录视觉、注册和找回密码入口；首页、观山、听雨、逐风及产品舞台只在登录后预览中显示。
- 验证：桌面和手机默认可见文本均不包含四个产品导航名称；`?state=member` 登录后预览恢复完整导航；两种状态无横向溢出和页面控制台错误。
- 产物：`design/previews/login-desktop.png`、`login-mobile.png`，并刷新登录后的 `home-desktop.png`、`home-mobile.png`。
- 边界：未连接真实认证 API，原型状态切换不能作为安全实现。

## 2026-08-01：移除网站软件图标

- 根因：登录页同心圆与大软件图标形成启动页感，并和超大标语争抢视觉中心；软件图标还会随客户端版本变化。
- 完成：访客品牌区改为纯文字 VisonCube 品牌场，缩小账户说明；顶部和登录后 Music 产品舞台移除图片，只保留名称与抽象视觉。
- 验证：访客态与登录后页面 `document.images.length` 均为 0；桌面/手机无横向溢出，手机品牌场与说明区域不重叠，页面控制台无错误；四张预览图已刷新。
- 边界：旧图片文件仍在 `prototype/assets/`，但页面不引用，未在本轮执行删除。

## 2026-08-01：登录页说明文案调整

- 完成：将“VISONCUBE ACCOUNT / 一个账户，统一进入。/ 统一账户服务在线”改为“VISONCUBE / 登录后继续访问 VisonCube。/ 账户服务正常”，避免口号化和重复强调统一账户；移除访客顶部空白长条的背景、边框、大圆角和阴影，保留独立文字品牌栏并收紧主体间距。
- 验证：桌面和手机真实浏览器渲染无横向溢出或文字遮挡；访客态不显示产品导航，图片元素为 0，页面控制台无错误；两张登录页预览图已刷新。
- 边界：仅修改静态原型文案，未接入真实认证 API，也未修改线上环境。

## 2026-08-01：登录后首页引导区精简

- 根因：原“观山，听雨，逐风。”巨幅口号和两个跳转按钮仍像宣传落地页，并与紧随其后的产品入口和顶部账户导航重复。
- 完成：改为左侧“选择一个产品。”、右侧账户共享提示的工作台式引导；删除英文长标签及“探索产品 / 进入账户”按钮，压缩引导区高度并上移产品舞台。
- 验证：桌面和手机真实浏览器均无横向溢出，手机标题保持单行；旧文案和按钮已不存在，图片元素为 0，页面控制台无错误；两张登录后首页预览图已刷新。
- 边界：仍为静态登录后预览，未接入认证跳转或产品路由。

## 2026-08-01：登录前后界面整体完善

- 目标：在不增加功能的前提下，统一登录入口和登录后产品首页的视觉语言与响应式细节。
- 完成：移除登录品牌区装饰编号、年份和多余英文，移除产品舞台仿窗口圆点、静态“服务在线”和重复产品索引；登录后标题改为纵向工作台层级；统一 24/16/10px 圆角、深色边框和阴影，补充导航、页签、按钮、链接和表单的 hover、focus-visible、active 状态。
- 可用性：手机目录按钮和登录页签达到至少 44px；辅助文字与输入提示对比度提升到至少 4.54:1；页面仍不引用软件图标或图片。
- 验证：登录前后桌面和手机真实浏览器渲染均无横向溢出，图片元素为 0、控制台错误为 0；JavaScript 语法、CSS 结构、废弃选择器和可见长破折号检查通过；四张预览图已刷新。
- 边界：未新增依赖、路由、API 或业务行为，`?state=member` 仍只用于静态预览。

## 2026-08-01：三个产品页与下载状态原型

- 完成：增加观山、听雨、逐风产品页，共用数据驱动详情结构，并分别使用山、雨、风抽象场景；下载区按平台显示公开版本和安装包类型，覆盖加载、失败重试、可下载和暂未发布状态。
- 验证：桌面 1440×1000 与手机 390×844 浏览器渲染无横向溢出，移动目录和失败重试可用，图片元素与控制台错误均为 0；输出六张产品页预览图。
- 边界：原型不请求生产 API、不触发真实下载；逐风保持暂未发布，等待 AI 正式发布包方案。

## 2026-08-01：正式 Vue Web 工程与账户接入

- 目标：将已确认的无依赖原型转换为正式 Vue Web 工程，并接入统一账户和发布服务。
- 完成：创建 Vue 3 + TypeScript + Vite 工程；拆分 API、认证状态、路由、布局、页面、邮箱验证、密码重置、账户和发布下载模块；使用 sessionStorage 保存 Bearer Session；产品区强制邮箱验证门禁。
- 完成：发布卡片按 `client_id` 请求服务器，处理加载、可下载、失败重试和 404 未发布；缺少 `display_version` 时不显示内部版本。
- 验证：`npm.cmd run typecheck` 通过；`npm.cmd run build` 通过；Vite 本地页面 HTTP 200；真实浏览器检查桌面/手机登录页、注册页、找回密码页和未登录路由门禁；控制台无警告或错误。
- 剩余：尚未部署线上；线上发布清单仍需补齐 TV、AI 和 Music 的公开版本字段；真实账户业务流程待手工验收。
