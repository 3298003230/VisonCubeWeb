# 当前进度

## 2026-08-29（观山 TV 1.4.5 网站与下载分发）
- 官网下载区新增 Android 手机、Android TV ARM64 和 Android TV `armeabi-v7a` 三个观山下载卡片；TV 1.4.5 两个包使用独立的 COS 目录 `VisonCube/TV/1.4.5/`。
- 生产发布清单已将 TV 两个 1.4.5 条目切换到 `download.sjmf.xyz`，旧 TV 包和旧发布清单已保留备份；手机版、Music 和 AI 条目未改变。
- `npm.cmd run build`、发布清单测试和公网 API、COS HTTP 200/206、完整 SHA-256 校验已通过；真实电视设备内更新与安装仍需单独验收。

## 2026-08-02（观山场景轻量化）
- 已移除正式样式和 prototype 样式对 `guanshan-scene.png` 的引用，观山恢复为与听雨、逐风一致的 CSS 抽象场景。
- 构建产物不再包含 1.6MB 山景图片，dist 总大小约 150KB。
- `npm.cmd run typecheck` 与 `npm.cmd run build` 已通过；线上已替换并备份旧版本。
- 线上备份：`/var/backups/visoncube-web-20260802-022818`；网站 HTTPS 200，API HTTPS 200。

## 2026-08-02（网站首次上线）
- 已将 Vue 生产构建部署到服务器 `/var/www/visoncube-web`，主域名为 `https://sjmf.xyz`。
- 已配置同源 `/api/` 反向代理到现有 `127.0.0.1:8088`，未修改 `linux.sjmf.xyz` 现有服务，也未加入 AI 下载链接。
- 已签发 `sjmf.xyz` HTTPS 证书并配置 HTTP 到 HTTPS 跳转。
- 验证：网站首页 HTTPS 200、Music 发布 API HTTPS 200、HTTP 301、JS/CSS 静态资源 HTTPS 200。

## 2026-08-02（观山视觉二次调整）
- 已将观山产品页从 CSS 几何占位场景改为真实山景氛围图，移除突兀的 16:9 框和原 CSS 山体。
- 新素材位于 `prototype/assets/guanshan-scene.png`，正式 Vue 样式与 prototype 样式均已接入。
- 验证：`npm.cmd run typecheck` 与 `npm.cmd run build` 通过，构建产物已包含图片资源。

## 2026-08-02
- 已优化“观山”产品页的 CSS 场景视觉，降低几何山体的生硬感，增强远山层次、雾光和播放框融合度。
- 同步更新正式 Vue 样式与 prototype 预览样式，未改变听雨、逐风的场景表现。
- 验证：`npm.cmd run typecheck` 与 `npm.cmd run build` 均通过。

## 已完成

- 固定首页、观山、听雨、逐风与三个软件的对应关系，并由 `PRODUCT.md`、`DESIGN.md` 记录范围、账户边界和视觉规范。
- 完成无依赖登录页和登录后首页原型；访客态隐藏产品导航，登录后首页使用“选择一个产品。”工作台式入口。
- 网站界面已移除软件图标和截图引用，只使用 VisonCube、观山、听雨、逐风名称与抽象视觉。
- 登录前后统一 24/16/10px 圆角、语义色、间距、focus-visible、active 和手机 44px 触控基线。
- 完成观山、听雨、逐风三个产品页原型；产品页复用同一数据驱动结构，分别使用山、雨、风抽象场景。
- 下载区按 `tv-android`、`music-android`、`music-windows`、`ai-windows` 映射平台，覆盖可下载、加载、失败重试和暂未发布状态，但不连接生产环境。
- 已生成登录、首页和三个产品页共十张桌面/手机预览图。
- 浏览器确认访客门禁、移动目录、失败重试、桌面/手机无横向溢出、图片元素为 0 和控制台错误为 0。
- 已创建 Vue 3、TypeScript、Vite 正式工程，拆分 API、认证状态、路由、布局、页面、发布下载组件和样式模块。
- 已接入密码登录、邮箱验证码登录、注册、找回密码、邮箱验证、账户信息、修改密码和退出登录流程。
- 已接入真实发布接口，公开版本缺失时不回退显示内部版本号；网站只使用接口返回的下载地址。
- 已完成正式工程的桌面/手机登录页视觉检查和未登录路由门禁检查，控制台无警告或错误。
- 已通过 TypeScript 类型检查和 Vite 生产构建，并添加依赖、构建产物和环境文件的忽略规则。

## 当前状态

- 正式 Web 工程已构建并部署到 `https://sjmf.xyz`，Nginx 将同源 `/api/` 转发到现有认证与发布服务。
- 当前下载清单由服务器接口提供：TV `1.3.8`，Music Android/Windows `2.13.0`；AI 下载入口继续保持未发布状态。
- 已验证网站首页、发布接口、HTTP 到 HTTPS 跳转以及静态资源均可访问。

## 推荐下一步

- 先用真实账户完成注册、邮箱验证、密码登录、邮箱验证码登录、找回密码和修改密码验收。
- 后续发布只需更新服务器发布清单和对应安装包，网站会通过接口同步最新链接。
