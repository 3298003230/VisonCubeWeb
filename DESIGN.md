# VisonCube Web 设计规范

## 1. 设计方向

设计登记为品牌型官网，账户区域后续单独按产品型界面处理。

参考方向为 [Raycast 官网](https://www.raycast.com/)：借鉴深色空间、克制边框、紧凑顶部导航、清晰文字层级和真实产品演示区。不得复制 Raycast 的品牌色、Logo、英文文案、键盘视觉或页面结构。

VisonCube 的设计主题为：

> 东方自然意象与数字产品相遇，山是观看，雨是聆听，风是智能与速度。

品牌语气词：沉静、精确、流动。

使用场景：用户在电脑或手机上寻找软件、确认版本或进入账户，环境可能明亮也可能偏暗；界面应在深色背景下保持清晰，而不是依赖纯黑和低对比灰字。

## 2. 首页叙事

### 未登录状态

- 顶部只保留 `VisonCube` 品牌，不出现“首页、观山、听雨、逐风”等产品导航。
- 主区域采用品牌视觉与登录表单并置，避免只放一个孤立表单造成大片空白。
- 登录表单优先展示密码登录，同时保留邮箱验证码登录、注册和找回密码入口。
- 未登录状态不提前展示产品舞台，登录成功后再进入完整主界面。
- 登录视觉只使用 `VisonCube` 文字品牌场，不显示任何软件图标。

### 登录后首页

登录后首页首屏不采用通用的“图标、标题、三张功能卡片”模板，使用一个统一产品舞台表达三个软件：

- 主标题：`选择一个产品。`
- 辅助说明：观山、听雨、逐风共用当前 VisonCube 账户。
- 产品舞台：观山、听雨、逐风以不同宽度和层级并置，听雨作为当前视觉中心，观山与逐风向两侧延伸。
- 产品舞台自身承担页面名、产品名和平台的对应关系，不在下方重复创建产品索引。
- 登录后界面采用工作台式信息层级，不使用宣传口号、重复跳转按钮、静态伪在线状态或无意义编号。

首页文案在用户确认前均为暂定内容。

## 3. 页面视觉分工

### 首页

- 近黑蓝背景，使用低亮度光晕建立空间，不使用纯黑。
- 珊瑚红作为主要交互强调色，冷蓝和雾青只用于产品舞台。
- 节奏沉稳，首屏信息完整，不制造大片空白。

### 观山（VisonCube-TV）

- 色彩：岩灰、雾蓝、松石绿。
- 构图：宽屏、横向、稳定，强调影视画面和电视观看距离。
- 动效：缓慢推近和景深，不使用快速漂浮。

### 听雨（VisonCube-Music）

- 色彩：墨蓝、雨青、暖橙。
- 构图：播放器界面、波形和歌词形成连续节奏。
- 动效：细小涟漪、波形和播放状态变化，不模拟夸张雨幕。

### 逐风（VisonCube-AI）

- 色彩：银灰、天空蓝、淡紫。
- 构图：流线、轨迹和识别区域表达速度与方向。
- 动效：短距离定向运动，不使用无意义随机粒子。

## 4. 产品页与下载区

- 产品页首屏由文字介绍和抽象产品场景左右组成；手机端改为上下排列，不使用软件图标或软件截图。
- 产品名称作为页面主标题，实际软件名称作为上方识别文字，平台与架构使用短标签表示。
- 下载区紧接产品首屏，不做成多个完全相同的悬浮卡片；每个平台使用一行信息，双平台产品用分隔线组织。
- 主动作必须写明平台，例如“下载 Android 版”“下载 Windows 版”，不得只写“立即下载”。
- 普通用户只看到 `display_version`；内部更新版本、最低兼容版本和强制更新标志不进入视觉主层级。
- 更新说明后续使用行内展开方式，不使用模态窗口。

下载状态固定为：

| 状态 | 文案与行为 |
| --- | --- |
| 加载中 | `正在获取最新版本…`，按钮禁用 |
| 可下载 | 显示公开版本、发布时间和下载按钮 |
| 获取失败 | `更新信息获取失败，请重新检查。`，提供“重新检查” |
| 暂未发布 | 显示对应平台暂未提供下载，按钮禁用 |

原型使用本地固定数据展示视觉，不请求生产 API，也不触发真实下载。正式工程只通过 `client_id` 获取发布信息，并使用返回的 `download_url` 作为按钮地址。

## 5. 色彩基线

使用 OKLCH 作为设计变量，十六进制仅作为旧浏览器回退。

| 角色 | OKLCH | 回退色 |
| --- | --- | --- |
| 页面背景 | `oklch(0.13 0.012 260)` | `#080a10` |
| 一级表面 | `oklch(0.18 0.014 260)` | `#11141c` |
| 二级表面 | `oklch(0.23 0.016 260)` | `#191d27` |
| 主文字 | `oklch(0.92 0.008 255)` | `#e8eaf0` |
| 次文字 | `oklch(0.69 0.014 255)` | `#9da2b0` |
| 主强调 | `oklch(0.68 0.18 25)` | `#f06465` |
| 听雨强调 | `oklch(0.73 0.11 218)` | `#5dc3d5` |
| 观山强调 | `oklch(0.67 0.09 155)` | `#63ac83` |
| 逐风强调 | `oklch(0.72 0.11 285)` | `#a28be0` |

文字禁止使用渐变填充。背景光晕可以使用低对比径向渐变，但不能影响文字对比度。

## 6. 字体与文字层级

- 原型阶段使用本机中文无衬线字体栈：`Segoe UI`、`PingFang SC`、`Microsoft YaHei`、系统字体。
- 正式开发前再确认是否引入可合法分发的品牌字体，不能依赖用户电脑未必安装的字体。
- 首屏标题使用强字重和紧缩字距，不使用渐变文字。
- 正文单行长度控制在 65 个汉字以内。
- 按钮、导航和状态文字必须保持可读，不使用过细字重。

## 7. 布局与组件

- 桌面内容最大宽度：`1180px`。
- 顶部导航使用独立的实色深色容器和 1px 边框，不使用大面积玻璃模糊。
- 页面圆角按职责分为三档：大面板 `24px`、产品卡片 `16px`、表单与交互控件 `10px`；手机端按同一比例收紧。
- 产品舞台允许不同宽度、不同高度和重叠关系，禁止三个完全相同的卡片。
- 产品区仅使用名称、排版、色彩和抽象状态表达，不使用可能随版本更换的软件图标或软件截图。
- 主按钮使用品牌珊瑚红，次按钮使用透明背景和边框。
- 账户页以后沿用同一色彩变量，但减少装饰、强化表单状态。
- 未登录顶部栏只包含品牌；登录表单输入框、页签、按钮、错误和加载状态使用同一组件语言。
- 导航、页签、按钮、链接和输入框必须具备清晰的 hover、focus-visible 与 active 状态；手机主要触控目标不低于 `44px`。
- 辅助文字和输入提示在对应深色表面上的对比度不低于 WCAG AA。

## 8. 响应式规则

- `>= 1024px`：完整顶部导航，三产品横向舞台。
- `640px - 1023px`：缩短导航间距，舞台仍保持横向但减少重叠。
- `< 640px`：隐藏桌面导航，保留品牌、当前页面和目录按钮；产品舞台改为纵向叠放。
- 手机端按钮至少 44px 高，文字不得缩小到 13px 以下。
- 不依赖悬停才能理解或进入页面。

## 9. 动效原则

- 动效用于表达页面切换、产品层级和状态反馈，不装饰性漂浮。
- 常规过渡 180 到 260ms，使用 `ease-out` 曲线。
- 不动画 `width`、`height` 等布局属性，优先使用 `transform` 和 `opacity`。
- 遵循 `prefers-reduced-motion`，关闭自动位移和循环波形。

## 10. 性能与资源

- 首屏优先使用压缩后的 WebP/AVIF、CSS 和轻量 SVG，不加载 4K 自动播放视频。
- 产品截图延迟加载；装饰图像必须设置尺寸，避免布局跳动。
- 首页脚本和样式首轮目标不超过 200KB（不含产品截图）。
- 原型不加载外部字体、分析脚本和第三方组件库。

## 11. 明确禁止

- 大片无内容的纯黑首屏；
- 三张完全相同的渐变功能卡片；
- 渐变文字；
- 默认玻璃拟态；
- 霓虹赛博朋克作为 AI 的固定表达；
- 把“观山、听雨、逐风”写成普通软件分类标签而没有视觉区别；
- 为模仿参考站而牺牲中文可读性。
- 在网站中展示当前软件图标或版本相关软件图片。
